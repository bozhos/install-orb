description: >
  Sample description install job
# What will this job do?
# Descriptions should be short, simple, and clear.

executor: default

parameters:
  dotnet_version:
    description: Select a specific version of the .NET Core framework.
    default: "3.1"
    type: string
steps:
  - install:
      to: << parameters.dotnet_version >>
